# crackle

TODO: project description, target site, and userscript metadata.

## Development

```
pnpm install
pnpm run debug   # local dev build + server
pnpm run build   # production build
pnpm run lint    # tsc + oxlint
pnpm run test    # ava tests
```
